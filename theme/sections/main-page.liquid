{%- capture json -%}
  {
  "section_id": {{ section.id | json }}
  }
{%- endcapture -%}

{{ json | strip_newlines | replace: "	", "" | replace: "  ", "" }}

{% schema %}
{
  "name": "t:sections.main-page.name"
}
{% endschema %}
